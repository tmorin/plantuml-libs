@startuml

!if ($INCLUSION_MODE == "remote")
  !include $LIB_BASE_LOCATION/dist/eventstorming/style.puml
!else
  !include style.puml
!endif

!procedure EsEntity($type, $stereotype, $id, $label="")
  !if ($label)
    $type "$label" as $id <<$stereotype>>
  !else
    $type $id <<$stereotype>>
  !endif
!endprocedure

@enduml
